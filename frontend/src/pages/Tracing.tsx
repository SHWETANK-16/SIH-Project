import{FormEvent,useState}from'react';import{useMutation}from'@tanstack/react-query';import{Search,Waypoints}from'lucide-react';import{useSearchParams}from'react-router-dom';import{tracingService}from'../services';import{DemoLabel,ErrorState,LoadingState,PageHeader,Panel}from'../components/ui';import{MoneyFlowView}from'../components/visuals/MoneyFlowView';
export default function Tracing(){const[params]=useSearchParams();const[id,setId]=useState(params.get('entity')||'TXN-0001');const trace=useMutation({mutationFn:tracingService.trace});const submit=(e:FormEvent)=>{e.preventDefault();trace.mutate(id)};return <><PageHeader eyebrow="TRACE" title="Money-flow tracing" description="Follow synthetic value movement across related accounts and transaction hops." actions={<DemoLabel/>}/><Panel title="Start a trace" subtitle="Enter a demonstration transaction or account reference"><form className="trace-form" onSubmit={submit}><label><span>Transaction ID / Account ID</span><div><Search/><input value={id} onChange={e=>setId(e.target.value.toUpperCase())} placeholder="TXN-0001"/></div></label><button className="primary-button" type="submit"><Waypoints/> Trace money</button></form></Panel>{trace.isPending&&<LoadingState/>}{trace.error&&<ErrorState error={trace.error}/>} {!trace.data&&!trace.isPending&&<div className="trace-prompt"><Waypoints/><h2>Trace a connected flow</h2><p>Try <button onClick={()=>trace.mutate('TXN-0001')}>TXN-0001</button> to explore a three-hop branching demonstration.</p></div>}{trace.data&&<Panel title="Trace results" subtitle={`${trace.data.hops.length} transfers identified · structured synthetic flow`}><MoneyFlowView flow={trace.data}/></Panel>}</>}
