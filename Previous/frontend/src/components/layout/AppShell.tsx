import{useState}from'react';import{NavLink,Outlet}from'react-router-dom';import{Activity,ChartNoAxesCombined,CircleDollarSign,FlaskConical,LayoutDashboard,Menu,Network,Search,ShieldCheck,Users,Waypoints,X}from'lucide-react';
const items=[['/dashboard','Overview',LayoutDashboard],['/networks','Network Investigation',Network],['/tracing','Money Flow',Waypoints],['/transactions','Transactions',CircleDollarSign],['/accounts','Accounts',Users],['/investigations','Investigations',ShieldCheck],['/simulation','Fraud Simulation',FlaskConical],['/model-performance','Model Performance',ChartNoAxesCombined],['/system-status','System Status',Activity]]as const;
export function AppShell(){const[open,setOpen]=useState(false);return <div className="shell"><aside className={open?'open':''}><div className="brand"><div className="brand-mark"><Waypoints/></div><div><strong>FIN<span>INTEL</span></strong><small>SIH 2026 · DEMO</small></div><button className="icon-button mobile-close" onClick={()=>setOpen(false)} aria-label="Close navigation"><X/></button></div><nav aria-label="Primary navigation">{items.map(([to,label,Icon])=><NavLink key={to} to={to} onClick={()=>setOpen(false)}><Icon/><span>{label}</span></NavLink>)}</nav><div className="sidebar-foot"><span className="pulse"/><div><strong>Mock engines ready</strong><small>In-memory baseline</small></div></div></aside><main><div className="topbar"><button className="icon-button menu" onClick={()=>setOpen(true)} aria-label="Open navigation"><Menu/></button><div className="search"><Search/><span>Search accounts, cases, transactions…</span><kbd>⌘ K</kbd></div><div className="top-status"><span className="pulse"/>SYSTEM READY</div><div className="avatar">IN</div></div><div className="content"><Outlet/></div></main>{open&&<button className="backdrop" aria-label="Close navigation" onClick={()=>setOpen(false)}/>}</div>}
