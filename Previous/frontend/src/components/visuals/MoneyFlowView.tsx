import type{MoneyFlow}from'../../types';import{ArrowDown,GitBranch}from'lucide-react';import{money,RiskBadge,date}from'../ui';
export function MoneyFlowView({flow}:{flow:MoneyFlow}){return <div className="flow-view"><div className="flow-summary"><div><span>Total traced</span><strong>{money(flow.total_traced)}</strong></div><div><span>Maximum depth</span><strong>{flow.max_depth} hops</strong></div><div><span>Trace reference</span><strong>{flow.trace_id}</strong></div></div><div className="flow-lanes">{flow.hops.map((h,i)=><article className={`flow-hop hop-${h.hop_number}`} key={h.transaction_id}><div className="hop-number">{i===0?<GitBranch/>:h.hop_number}</div><div className="flow-account"><small>SOURCE</small><strong>{h.source}</strong></div><div className="flow-arrow"><span>{money(h.amount)}</span><ArrowDown/></div><div className="flow-account destination"><small>DESTINATION</small><strong>{h.destination}</strong></div><div className="hop-meta"><RiskBadge level={h.risk_level}/><span>{date(h.timestamp)}</span><code>{h.transaction_id}</code></div></article>)}</div></div>}
