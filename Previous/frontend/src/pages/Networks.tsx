import{useFlexibleQuery as useQuery}from'../utils/query';import{ChevronRight,Network as NetIcon}from'lucide-react';import{Link,useParams}from'react-router-dom';import{networkService}from'../services';import{DemoLabel,ErrorState,LoadingState,PageHeader,Panel,RiskBadge,money}from'../components/ui';import{NetworkGraph}from'../components/visuals/NetworkGraph';
export default function Networks(){const{id}=useParams();const q=useQuery({queryKey:['networks',id],queryFn:()=>id?networkService.get(id):networkService.list()});if(q.isLoading)return <LoadingState/>;if(q.error)return <ErrorState error={q.error}/>;if(id){const net=q.data as Awaited<ReturnType<typeof networkService.get>>;return <><PageHeader eyebrow="DISCOVER · CONNECT" title={net.name} description={`${net.network_id} · Potential network investigation workspace`} actions={<DemoLabel/>}/><div className="network-kpis"><div><span>Network risk</span><strong>{net.risk_score}/100</strong><RiskBadge level={net.risk_level}/></div><div><span>Connected entities</span><strong>{net.node_count}</strong><small>{net.edge_count} observed links</small></div><div><span>Estimated flow</span><strong>{money(net.estimated_flow)}</strong><small>Synthetic exposure</small></div><div><span>Discovered</span><strong>{new Date(net.discovered_at).toLocaleDateString('en-IN',{day:'2-digit',month:'short'})}</strong><small>Monitoring active</small></div></div><Panel title="Network intelligence map" subtitle="Select an entity to inspect its synthetic signals"><NetworkGraph network={net}/></Panel></>};const nets=q.data as Awaited<ReturnType<typeof networkService.list>>;return <><PageHeader title="Network investigation" description="Prioritized suspicious clusters discovered across the synthetic event graph." actions={<DemoLabel/>}/><div className="network-list">{nets.map(n=><Link to={`/networks/${n.network_id}`} className="network-card" key={n.network_id}><div className="network-card-icon"><NetIcon/></div><div><span className="eyebrow">{n.network_id}</span><h2>{n.name}</h2><p>{n.key_indicators.slice(0,2).join(' · ')}</p></div><div className="network-card-stats"><span><b>{n.node_count}</b> entities</span><span><b>{money(n.estimated_flow)}</b> flow</span></div><RiskBadge level={n.risk_level}/><ChevronRight/></Link>)}</div></>}
