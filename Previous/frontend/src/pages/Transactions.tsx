import{useState}from'react';import{useQuery}from'@tanstack/react-query';import{Download,Search}from'lucide-react';import{transactionService}from'../services';import{DemoLabel,EmptyState,ErrorState,LoadingState,PageHeader,RiskBadge,date,money}from'../components/ui';
export default function Transactions(){const q=useQuery({queryKey:['transactions'],queryFn:transactionService.list});const[search,setSearch]=useState('');const[risk,setRisk]=useState('ALL');if(q.isLoading)return <LoadingState/>;if(q.error)return <ErrorState error={q.error}/>;const rows=q.data!.filter(x=>(risk==='ALL'||x.risk_level===risk)&&Object.values(x).some(v=>String(v).toLowerCase().includes(search.toLowerCase())));return <><PageHeader title="Transaction explorer" description="Inspect connected synthetic transfers, risk bands and network context." actions={<><DemoLabel/><button className="secondary-button"><Download/>Export view</button></>}/><div className="filter-bar"><label className="search-input"><Search/><input aria-label="Search transactions" placeholder="Search ID or account…" value={search} onChange={e=>setSearch(e.target.value)}/></label><select value={risk} onChange={e=>setRisk(e.target.value)} aria-label="Filter risk"><option value="ALL">All risk levels</option><option>CRITICAL</option><option>HIGH</option><option>MEDIUM</option><option>LOW</option></select><select aria-label="Transaction type"><option>All transaction types</option><option>UPI</option><option>IMPS</option><option>NEFT</option><option>RTGS</option></select><span>{rows.length} results</span></div><div className="panel table-panel">{!rows.length?<EmptyState/>:<div className="table-scroll"><table><thead><tr><th>Time</th><th>Transaction</th><th>Source</th><th>Destination</th><th>Amount</th><th>Risk</th><th>Status</th><th>Type</th><th>Network</th></tr></thead><tbody>{rows.slice(0,50).map(x=><tr key={x.transaction_id}><td>{date(x.timestamp)}</td><td><strong className="mono">{x.transaction_id}</strong></td><td>{x.source_account_id}</td><td>{x.destination_account_id}</td><td><strong>{money(x.amount)}</strong></td><td><RiskBadge level={x.risk_level}/></td><td><span className="status-text">{x.status}</span></td><td>{x.transaction_type}</td><td>{x.network_id}</td></tr>)}</tbody></table></div>}<footer className="table-footer">Showing 1–{Math.min(50,rows.length)} of {rows.length} synthetic records</footer></div></>}
