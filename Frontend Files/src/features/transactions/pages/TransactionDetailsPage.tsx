import { ArrowLeft, ArrowRight, Building2, CircleAlert, FileSearch, Landmark, Network, ShieldAlert } from 'lucide-react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { ErrorState, LoadingState } from '../../../shared/components/States';
import { StatusPill } from '../../../shared/components/StatusPill';
import { useTransaction } from '../hooks';

function formatAmount(amount: number) { return new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 }).format(amount); }

export function TransactionDetailsPage() {
  const { id = '' } = useParams(); const { data: transaction, isLoading, isError, refetch } = useTransaction(id); const navigate = useNavigate();
  if (isLoading) return <LoadingState label="Loading transaction record..." />;
  if (isError || !transaction) return <ErrorState message="Unable to load this transaction." onRetry={() => void refetch()} />;
  return <div className="page-container"><Link to="/transactions" className="back-link in-app"><ArrowLeft size={16} /> Transaction Explorer</Link><div className="details-hero"><div><p className="eyebrow">Transaction record</p><h1>{transaction.id}</h1><p>{transaction.category} · {new Intl.DateTimeFormat('en-IN', { dateStyle: 'full', timeStyle: 'short' }).format(new Date(transaction.date))}</p></div><div className="details-hero-actions"><StatusPill status={transaction.status} /><button className="button button-primary" onClick={() => navigate(`/investigations/${transaction.id}`)}>Investigate Transaction <ArrowRight size={16} /></button></div></div><div className="detail-layout"><section className="detail-main"><article className="content-card money-flow-card"><div className="card-heading"><div><p className="eyebrow">Funds movement</p><h2>Transaction pathway</h2></div><span className={`risk-score risk-${transaction.risk.level}`}><i /> Risk {transaction.risk.score}</span></div><div className="flow-visual"><div className="flow-party"><span className="entity-icon"><Building2 size={22} /></span><div><small>Sender</small><strong>{transaction.sender}</strong><p>{transaction.senderBank}</p></div></div><div className="flow-line"><span>{formatAmount(transaction.amount)}</span><i><ArrowRight size={19} /></i></div><div className="flow-party"><span className="entity-icon recipient"><Landmark size={22} /></span><div><small>Receiver</small><strong>{transaction.receiver}</strong><p>{transaction.receiverBank}</p></div></div></div></article><article className="content-card"><div className="card-heading"><div><p className="eyebrow">Connected intelligence</p><h2>Related transactions</h2></div><Link to="/transactions" className="text-link">View all</Link></div><div className="related-list"><div><span className="mini-dot high" /><div><b>TXN-6N09A</b><p>Linked recipient network · ₹36.8 L</p></div><span>Today, 08:56</span></div><div><span className="mini-dot medium" /><div><b>TXN-5R22J</b><p>Shared account device signature · ₹4.48 L</p></div><span>Yesterday</span></div><div><span className="mini-dot high" /><div><b>TXN-7F11C</b><p>Beneficiary bridge detected · ₹11.2 L</p></div><span>24 Aug</span></div></div></article></section><aside className="detail-aside"><article className="content-card risk-panel"><ShieldAlert size={24} /><p className="eyebrow">Risk assessment</p><strong>{transaction.risk.score}<small>/100</small></strong><span className={`status-pill status-${transaction.risk.level}`}>{transaction.risk.level} risk</span><div className="risk-meter"><i style={{ width: `${transaction.risk.score}%` }} /></div></article><article className="content-card"><div className="card-heading"><h2>Risk indicators</h2></div><ul className="indicator-list">{transaction.risk.indicators.map((indicator) => <li key={indicator}><CircleAlert size={15} />{indicator}</li>)}</ul></article><article className="content-card action-card"><Network size={20} /><div><h3>Network relationships</h3><p>6 connected entities identified in the investigation graph.</p></div><button className="text-link" onClick={() => navigate(`/investigations/${transaction.id}`)}>Open network <ArrowRight size={14} /></button></article></aside></div></div>;
}
