import { Maximize2, Minus, MousePointer2, Plus, RotateCcw } from 'lucide-react';
import { useState, type PointerEvent, type WheelEvent } from 'react';
import type { NetworkEdge, NetworkNode } from '../../../types/common';

const riskColor = { low: '#4cbf7e', medium: '#d4ae49', high: '#dd8143', critical: '#d55358' } as const;
interface Viewport { x: number; y: number; scale: number; }

export function InvestigationGraph({ nodes, edges, selectedNode, onSelect }: { nodes: NetworkNode[]; edges: NetworkEdge[]; selectedNode: string; onSelect: (id: string) => void }) {
  const [view, setView] = useState<Viewport>({ x: 0, y: 0, scale: 1 });
  const [dragStart, setDragStart] = useState<{ x: number; y: number; view: Viewport } | null>(null);
  const findNode = (id: string) => nodes.find((node) => node.id === id)!;
  const zoom = (delta: number) => setView((current) => ({ ...current, scale: Math.min(1.55, Math.max(.62, current.scale + delta)) }));
  const onWheel = (event: WheelEvent<SVGSVGElement>) => { event.preventDefault(); zoom(event.deltaY < 0 ? .1 : -.1); };
  const onPointerMove = (event: PointerEvent<SVGSVGElement>) => { if (dragStart) setView({ ...dragStart.view, x: dragStart.view.x + event.clientX - dragStart.x, y: dragStart.view.y + event.clientY - dragStart.y }); };
  return <article className="content-card graph-card"><div className="card-heading"><div><p className="eyebrow">Relationship mapping</p><h2>Financial intelligence network</h2></div><span className="graph-hint"><MousePointer2 size={14} /> Scroll to zoom · Drag to pan</span></div><div className="graph-canvas"><svg className="investigation-graph" viewBox="0 0 760 440" onWheel={onWheel} onPointerDown={(event) => setDragStart({ x: event.clientX, y: event.clientY, view })} onPointerMove={onPointerMove} onPointerUp={() => setDragStart(null)} onPointerLeave={() => setDragStart(null)}><defs><marker id="arrow" markerWidth="8" markerHeight="8" refX="5" refY="3" orient="auto"><path d="M0,0 L0,6 L6,3 z" fill="#5b936b" /></marker><filter id="nodeglow"><feGaussianBlur stdDeviation="3" result="blur" /><feMerge><feMergeNode in="blur" /><feMergeNode in="SourceGraphic" /></feMerge></filter></defs><g transform={`translate(${view.x} ${view.y}) scale(${view.scale})`}>{edges.map((edge) => { const source = findNode(edge.source); const target = findNode(edge.target); return <line key={edge.id} x1={source.x} y1={source.y} x2={target.x} y2={target.y} className={`network-edge edge-${edge.risk}`} markerEnd="url(#arrow)" />; })}{nodes.map((node) => { const active = selectedNode === node.id; const radius = node.id === 'main' ? 30 : node.type === 'bank' ? 16 : 21; return <g key={node.id} className={`network-graph-node ${active ? 'selected' : ''}`} onClick={(event) => { event.stopPropagation(); onSelect(node.id); }} role="button" tabIndex={0} aria-label={`Select ${node.label}`} onKeyDown={(event) => { if (event.key === 'Enter') onSelect(node.id); }}><circle cx={node.x} cy={node.y} r={radius + (active ? 6 : 0)} fill={active ? `${riskColor[node.risk]}26` : 'transparent'} /><circle cx={node.x} cy={node.y} r={radius} fill="#10261a" stroke={riskColor[node.risk]} strokeWidth={node.id === 'main' ? 3 : 2} filter={node.id === 'main' ? 'url(#nodeglow)' : undefined} /><circle cx={node.x} cy={node.y} r={node.id === 'main' ? 9 : 5} fill={riskColor[node.risk]} /><text x={node.x} y={node.y + radius + 15} textAnchor="middle">{node.label}</text>{node.value && <text className="node-value" x={node.x} y={node.y + radius + 28} textAnchor="middle">{node.value}</text>}</g>; })}</g></svg><div className="graph-controls"><button className="icon-button" onClick={() => zoom(.1)} aria-label="Zoom in"><Plus size={16} /></button><button className="icon-button" onClick={() => zoom(-.1)} aria-label="Zoom out"><Minus size={16} /></button><button className="icon-button" onClick={() => setView({ x: 0, y: 0, scale: 1 })} aria-label="Reset graph"><RotateCcw size={15} /></button><button className="icon-button" aria-label="Fullscreen graph"><Maximize2 size={15} /></button></div></div><div className="network-legend"><span><i className="legend-dot low" /> Low risk</span><span><i className="legend-dot medium" /> Medium risk</span><span><i className="legend-dot high" /> High risk</span><span><i className="legend-dot critical" /> Critical risk</span></div></article>;
}
